# FlowScript Environment Variables

# Server Configuration
PORT=3013

# Google OAuth2 Configuration
# To get these values:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select an existing one
# 3. Enable the Gmail API and Google Drive API
# 4. Create OAuth 2.0 credentials
# 5. Add http://localhost:3013/api/auth/google/callback to authorized redirect URIs
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:3013/api/auth/google/callback

# Database Configuration
TOKEN_DATABASE_PATH=tokens.sqlite